# Repeat Tutor

Flutter приложение для изучения языков с интерфейсом чата в стиле Cupertino.

## 📱 Описание

Repeat Tutor - это минималистичное приложение для изучения языков, построенное с использованием Flutter и Riverpod. Приложение использует интерфейс в стиле iOS (Cupertino) для максимально нативного пользовательского опыта.

## 🏗️ Архитектура

Приложение построено на основе **Clean Architecture** с использованием **Feature-based** структуры и **MVVM** паттерна. Управление состоянием осуществляется через **Riverpod**.

### Архитектурные слои

```text
┌─────────────────────────────────────┐
│      Presentation Layer              │  ← Экраны, виджеты (features/*/presentation)
├─────────────────────────────────────┤
│      Logic Layer                     │  ← State managers (features/*/logic)
├─────────────────────────────────────┤
│      Use Case Layer                  │  ← Бизнес-логика (features/*/use_case)
├─────────────────────────────────────┤
│      Domain Layer                    │  ← Модели, интерфейсы (core/domain)
├─────────────────────────────────────┤
│      Data Layer                      │  ← Репозитории, DAO (core/data, core/database)
├─────────────────────────────────────┤
│      Infrastructure Layer            │  ← DI, сервисы, провайдеры (infrastructure)
└─────────────────────────────────────┘
```

### Структура проекта

```text
lib/
├── core/                    # Общие компоненты приложения
│   ├── ab_test/            # A/B тестирование и подписки
│   ├── audio/              # Аудио сервисы
│   ├── data/               # Data слой
│   │   ├── mappers/        # Мапперы для преобразования данных
│   │   └── repositories/   # Реализации репозиториев
│   ├── database/           # База данных SQLite
│   │   ├── daos/           # Data Access Objects
│   │   ├── tables.dart  # Определение таблиц
│   │   └── app_database.dart
│   ├── domain/             # Domain слой
│   │   ├── enums/          # Перечисления (Language, DifficultyLevel)
│   │   ├── models/         # Доменные модели (Chat, Message, RealtimeSession)
│   │   └── repositories/   # Интерфейсы репозиториев
│   ├── gpt/                # GPT сервис для AI репетитора
│   │   ├── instructions/   # Инструкции для AI
│   │   └── gpt_service.dart
│   ├── localization/       # Локализация (10 языков)
│   ├── logging/            # Система логирования
│   ├── permission/          # Управление разрешениями
│   ├── plugins/            # Обертки для плагинов
│   ├── presentation/       # Общие UI компоненты
│   ├── realtime/           # WebRTC для голосовых звонков
│   ├── router/             # Навигация (Auto Route)
│   ├── speech/             # Распознавание речи
│   ├── theme/              # Тема приложения
│   └── utils/              # Утилиты
│
├── features/                # Feature-based модули
│   ├── chats/              # Модуль чатов
│   │   ├── logic/          # State managers (ChatNotifier, ChatListNotifier)
│   │   ├── presentation/   # Экраны и виджеты
│   │   │   ├── components/ # Переиспользуемые компоненты
│   │   │   ├── chat_list_screen.dart
│   │   │   └── chat_screen.dart
│   │   └── use_case/       # Use cases (CreateChat, AddMessage, DeleteChat)
│   ├── home/               # Главный экран
│   ├── onboarding/         # Онбординг
│   ├── paywall/            # Экран подписки
│   ├── profile/            # Профиль пользователя
│   ├── realtime_call/      # Голосовые звонки
│   └── statistics/          # Статистика
│
├── infrastructure/         # Infrastructure слой
│   ├── core.dart          # Провайдеры сервисов (GPT, WebRTC, Database)
│   ├── repositories.dart   # Провайдеры репозиториев
│   ├── state_managers.dart # Провайдеры state managers
│   └── use_case.dart      # Провайдеры use cases
│
├── gen/                    # Сгенерированный код
│   ├── assets.gen.dart    # Генерация ассетов
│   └── fonts.gen.dart     # Генерация шрифтов
│
└── main.dart               # Точка входа приложения
```

### Принципы архитектуры

#### Feature-based структура

Каждая фича приложения изолирована в отдельной папке и содержит:

- **`logic/`** - State managers (Notifiers) для управления состоянием фичи
- **`presentation/`** - UI компоненты (экраны, виджеты)
- **`use_case/`** - Бизнес-логика фичи (Use Cases)

#### Clean Architecture

- **Domain Layer** (`core/domain/`) - чистые модели и интерфейсы, без зависимостей
- **Data Layer** (`core/data/`, `core/database/`) - реализация репозиториев и работа с БД
- **Presentation Layer** (`features/*/presentation/`) - UI компоненты
- **Infrastructure Layer** (`infrastructure/`) - DI и провайдеры

#### Dependency Injection

Все зависимости централизованы в `infrastructure/`:

- **`core.dart`** - провайдеры сервисов (GPT, WebRTC, Database, Audio, Speech)
- **`repositories.dart`** - провайдеры репозиториев
- **`state_managers.dart`** - провайдеры state managers
- **`use_case.dart`** - провайдеры use cases

#### Управление состоянием

- **Riverpod** - для управления состоянием
- **ChangeNotifier** - для реактивных обновлений UI
- **State managers** - изолированы в каждой фиче
- **Use Cases**** - для бизнес-логики, изолированной от UI

## 🛠️ Технологии

### Основные

- **Flutter** - UI фреймворк
- **Riverpod** - Управление состоянием
- **Cupertino** - iOS-стиль интерфейса
- **Auto Route** - Навигация и роутинг

### База данных

- **Drift** - ORM для SQLite
- **SQLite3** - Локальная база данных

### Сеть и API

- **Dio** - HTTP клиент для REST API
- **WebRTC** - Голосовые звонки в реальном времени
- **OpenAI GPT** - AI репетитор для чатов и звонков

### Аудио

- **just_audio** - Воспроизведение аудио
- **record** - Запись аудио
- **speech_to_text** - Распознавание речи

### Локализация

- **Flutter Localizations** - Многоязычность
- **intl** - Интернационализация

### UI компоненты

- **lucide_icons** - Иконки
- **markdown_widget** - Отображение Markdown
- **flutter_animate** - Анимации
- **showcaseview** - Подсказки и онбординг

### Монетизация

- **Apphud** - Управление подписками
- **ab_test_service** - A/B тестирование

### Утилиты

- **path_provider** - Работа с путями файловой системы
- **shared_preferences** - Локальное хранилище настроек
- **permission_handler** - Управление разрешениями
- **wakelock_plus** - Предотвращение блокировки экрана
- **package_info_plus** - Информация о приложении
- **share_plus** - Поделиться контентом

## 🚀 Запуск

1. Убедитесь, что у вас установлен Flutter SDK
2. Установите зависимости:

   ```bash
   flutter pub get
   ```

3. Запустите приложение:

   ```bash
   flutter run
   ```

## 📋 Функциональность

### ✅ Основные возможности

#### 💬 Текстовый чат с AI-репетитором

- **Создание чатов** на различные темы для изучения языка
- **Текстовые сообщения** с AI-репетитором в стиле мессенджера
- **Управление чатами** - создание, просмотр, удаление
- **История сообщений** - все переписки сохраняются локально
- **Автоматическая прокрутка** к новым сообщениям
- **Удаление сообщений** из чата

#### 🎤 Голосовые звонки (Realtime Call)

- **Голосовые звонки** с AI-репетитором через WebRTC
- **Реальное время** - двусторонняя голосовая связь
- **Коррекция речи** - AI исправляет ошибки в произношении и грамматике
- **Визуальная обратная связь** - отображение сообщений репетитора и исправлений
- **Управление микрофоном** - включение/выключение звука
- **Таймер звонка** - отслеживание длительности сессии
- **Уровни громкости** - визуализация аудио уровней пользователя и репетитора

#### 📚 Настройки обучения

- **Выбор языка обучения** - поддержка 9 языков:
  - Японский (日本語)
  - Португальский (Европа и Бразилия)
  - Испанский (Español)
  - Французский (Français)
  - Итальянский (Italiano)
  - Немецкий (Deutsch)
  - Русский
  - Английский (English)
- **Уровни сложности** - от Beginner (A1) до Proficiency (C2)
- **Язык репетитора** - выбор языка, на котором репетитор общается
- **Язык интерфейса** - настройка языка приложения

#### 📊 Статистика и профиль

- **Статистика звонков** - отслеживание времени практики:
  - Время за сегодня
  - Общее время практики
- **Настройки профиля** - управление языковыми настройками
- **Персонализация** - сохранение предпочтений пользователя

#### 🎯 Онбординг

- **Экран приветствия** - знакомство с приложением
- **Обучение использованию** - подсказки по основным функциям
- **Адаптация** - помощь в настройке первого чата

#### 💳 Подписка (Paywall)

- **Экран подписки** - интеграция с системой подписок
- **A/B тестирование** - оптимизация конверсии через ab_test_service

#### 🌍 Локализация

- **Многоязычный интерфейс** - поддержка 10 языков интерфейса:
  - Русский
  - Английский
  - Немецкий
  - Испанский
  - Французский
  - Итальянский
  - Японский
  - Португальский (PT и BR)
- **Автоматическая генерация** - локализация генерируется автоматически

#### 💾 Локальное хранение

- **База данных SQLite** - все чаты и сообщения хранятся локально
- **Офлайн режим** - доступ к истории чатов без интернета
- **Синхронизация** - автоматическое сохранение новых сообщений

### 🔄 Управление состоянием

Приложение использует **Riverpod** для управления состоянием с паттерном **MVVM**.

#### State Managers (Notifiers)

State managers находятся в `features/*/logic/` и управляют состоянием конкретной фичи:

- **`ChatListNotifier`** - управление списком чатов
- **`ChatNotifier`** - управление сообщениями в чате
- **`RealtimeCallNotifier`** - управление состоянием голосового звонка
- **`ProfileNotifier`** - управление настройками профиля
- **`HomeScreenNotifier`** - управление главным экраном
- **`OnboardingNotifier`** - управление онбордингом
- **`PaywallChangeNotifier`** - управление подпиской

#### Use Cases

Бизнес-логика изолирована в Use Cases в `features/*/use_case/`:

- **`CreateChatUseCase`** - создание нового чата
- **`AddMessageUseCase`** - добавление сообщения в чат
- **`DeleteChatUseCase`** - удаление чата
- **`StartRealtimeCallUseCase`** - запуск голосового звонка
- **`ProfileSettingsUseCase`** - управление настройками профиля
- **`PurchaseUseCase`** - покупка подписки

#### Провайдеры

Все провайдеры централизованы в `infrastructure/`:

- **`state_managers.dart`** - провайдеры для всех Notifiers
- **`use_case.dart`** - провайдеры для всех Use Cases
- **`repositories.dart`** - провайдеры для репозиториев
- **`core.dart`** - провайдеры для сервисов

### 🎨 Дизайн

- **Минималистичный** интерфейс в стиле Cupertino
- **Адаптивные** компоненты под размер экрана
- **Нативные** жесты и анимации iOS
- **Темная/светлая** тема (системная)

## 🏛️ Dependency Injection

Все зависимости централизованы в папке `infrastructure/` и разделены по назначению:

### Структура DI

- **`core.dart`** - провайдеры сервисов:
  - `databaseProvider` - база данных SQLite
  - `gptServiceProvider` - GPT сервис для AI
  - `realtimeWebRTCConnectionProvider` - WebRTC соединение
  - `abTestServiceProvider` - A/B тестирование
  - `speechRecognizerProvider` - распознавание речи
  - `audioServiceProvider` - аудио сервис
  - `localStorageProvider` - локальное хранилище
  - `dioProvider` - HTTP клиент
  - `routerProvider` - навигация

- **`repositories.dart`** - провайдеры репозиториев:
  - `chatRepositoryProvider` - репозиторий чатов

- **`state_managers.dart`** - провайдеры state managers:
  - `chatListNotifierProvider` - список чатов
  - `chatNotifierProvider` - чат
  - `realtimeCallProvider` - голосовой звонок
  - `profileProvider` - профиль
  - `homeScreenNotifierProvider` - главный экран
  - `onboardingNotifierProvider` - онбординг
  - `paywallChangeNotifierProvider` - подписка

- **`use_case.dart`** - провайдеры use cases:
  - `createChatUseCaseProvider` - создание чата
  - `addMessageUseCaseProvider` - добавление сообщения
  - `deleteChatUseCaseProvider` - удаление чата
  - `startRealtimeCallUseCaseProvider` - запуск звонка
  - `profileSettingsUseCaseProvider` - настройки профиля
  - `purchaseUseCaseProvider` - покупка подписки
  - `openScreenUseCaseProvider` - навигация
  - `initializeUseCaseProvider` - инициализация приложения

### Использование

```dart
import '../../infrastructure/core.dart';
import '../../infrastructure/state_managers.dart';
import '../../infrastructure/use_case.dart';

class ChatScreen extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    // Использование state manager
    final chatNotifier = ref.watch(chatNotifierProvider);
    
    // Использование use case
    final createChatUseCase = ref.read(createChatUseCaseProvider);
    
    // Использование сервиса
    final gptService = ref.watch(gptServiceProvider);
    
    // ...
  }
}
```

### Преимущества DI структуры

1. **Централизация** - все зависимости в одном месте (`infrastructure/`)
2. **Разделение по назначению** - провайдеры сгруппированы логически
3. **Масштабируемость** - легко добавлять новые провайдеры
4. **Тестируемость** - простое мокирование через Riverpod overrides
5. **Типобезопасность** - все провайдеры типизированы
6. **Чистота кода** - четкое разделение ответственности

## 📱 Экраны

### Главный экран (Home)

- **Навигация** между основными разделами через нижнюю панель
- **Вкладки**: Чаты и Профиль
- **Кнопка создания чата** - плавающая кнопка для быстрого доступа
- **Онбординг** - автоматическое отображение при первом запуске

### Список чатов (Chat List)

- **Отображение всех чатов** с последними сообщениями
- **Индикаторы непрочитанных сообщений**
- **Кнопка добавления нового чата**
- **Навигация к экрану чата** по нажатию
- **Удаление чатов** свайпом
- **Состояния загрузки** - shimmer эффекты при загрузке
- **Пустое состояние** - подсказки при отсутствии чатов

### Экран чата (Chat)

- **Интерфейс переписки** с пузырьками сообщений
- **Поле ввода** с кнопкой отправки
- **Кнопка голосового звонка** - быстрый переход к Realtime Call
- **Автоматическая прокрутка** к новым сообщениям
- **Удаление сообщений**
- **Градиентный фон** для визуального разделения

### Голосовой звонок (Realtime Call)

- **Интерфейс звонка** с аватаром языка
- **Таймер звонка** - отображение длительности сессии
- **Сообщения репетитора** - текстовая обратная связь
- **Коррекции в Markdown** - визуальное отображение исправлений
- **Управление микрофоном** - включение/выключение звука
- **Кнопка завершения звонка**
- **Визуализация аудио уровней** - анимация при разговоре
- **Обработка ошибок** - отображение ошибок соединения

### Профиль (Profile)

- **Настройки языков**:
  - Язык интерфейса приложения
  - Язык для изучения
  - Язык репетитора
- **Статистика практики**:
  - Время за сегодня
  - Общее время практики
- **Выбор языков** через Cupertino Picker

### Онбординг (Onboarding)

- **Экран приветствия** - знакомство с приложением
- **Обучение использованию** - подсказки по основным функциям
- **Адаптация** - помощь в настройке первого чата

### Подписка (Paywall)

- **Экран подписки** - интеграция с системой подписок
- **A/B тестирование** - оптимизация конверсии

## 🔧 Разработка

### Добавление новой фичи

1. Создайте папку `lib/features/your_feature/`
2. Создайте структуру:
   - `logic/` - State managers (Notifiers)
   - `presentation/` - Экраны и виджеты
   - `use_case/` - Use cases для бизнес-логики
3. Добавьте провайдеры в `infrastructure/`:
   - State managers → `state_managers.dart`
   - Use cases → `use_case.dart`
   - Репозитории → `repositories.dart` (если нужен)
   - Сервисы → `core.dart` (если нужен)

### Добавление нового провайдера

#### State Manager

1. Создайте Notifier в `features/your_feature/logic/`
2. Добавьте провайдер в `infrastructure/state_managers.dart`:

```dart
final yourFeatureNotifierProvider = ChangeNotifierProvider((ref) {
  return YourFeatureNotifier(
    repository: ref.watch(yourRepositoryProvider),
  );
});
```

#### Use Case

1. Создайте Use Case в `features/your_feature/use_case/`
2. Добавьте провайдер в `infrastructure/use_case.dart`:

```dart
final yourUseCaseProvider = Provider((ref) {
  return YourUseCase(ref: ref);
});
```

#### Сервис

1. Создайте сервис в `core/your_service/`
2. Добавьте провайдер в `infrastructure/core.dart`:

```dart
final yourServiceProvider = Provider<YourService>((ref) {
  return YourServiceImpl();
});
```

### Добавление нового репозитория

1. Создайте интерфейс в `core/domain/repositories/`
2. Создайте реализацию в `core/data/repositories/`
3. Добавьте провайдер в `infrastructure/repositories.dart`:

```dart
final yourRepositoryProvider = Provider<YourRepository>((ref) {
  final database = ref.watch(databaseProvider);
  return YourRepositoryImpl(database);
});
```

## 📄 Лицензия

Этот проект создан для образовательных целей.
