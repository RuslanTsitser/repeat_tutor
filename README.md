# Repeat Tutor

Flutter приложение для изучения языков с интерфейсом чата в стиле Cupertino.

## 📱 Описание

Repeat Tutor - это минималистичное приложение для изучения языков, построенное с использованием Flutter и Riverpod. Приложение использует интерфейс в стиле iOS (Cupertino) для максимально нативного пользовательского опыта.

## 🏗️ Архитектура

### Структура проекта

```text
lib/
├── infrastructure/di/     # Infrastructure Layer
│   └── di.dart           # Riverpod провайдеры
├── presentation/         # Presentation Layer
│   ├── notifiers/        # Стейт менеджеры
│   │   ├── chat_notifier.dart      # ChatNotifier
│   │   ├── message_notifier.dart   # MessageNotifier
│   │   └── notifiers.dart          # Экспорт notifiers
│   ├── screens/          # Экраны приложения
│   │   ├── chat_list_screen.dart  # Список чатов
│   │   ├── chat_screen.dart       # Экран чата
│   │   └── screens.dart           # Экспорт экранов
│   ├── widgets/          # Переиспользуемые виджеты
│   │   ├── chat_list_item.dart    # Элемент списка чатов
│   │   ├── message_bubble.dart    # Пузырек сообщения
│   │   ├── message_input.dart     # Поле ввода сообщений
│   │   └── widgets.dart           # Экспорт виджетов
│   └── presentation.dart # Главный экспорт presentation
├── domain/               # Domain Layer
│   ├── models/           # Модели данных
│   │   ├── chat.dart     # Модель чата
│   │   ├── message.dart  # Модель сообщения
│   │   └── models.dart   # Экспорт моделей
│   └── domain.dart       # Главный экспорт domain
├── localization/         # Локализация
└── main.dart             # Точка входа
```

## 🛠️ Технологии

- **Flutter** - UI фреймворк
- **Riverpod** - Управление состоянием
- **Cupertino** - iOS-стиль интерфейса
- **Equatable** - Сравнение объектов
- **Flutter Localizations** - Многоязычность

## 🚀 Запуск

1. Убедитесь, что у вас установлен Flutter SDK
2. Установите зависимости:

   ```bash
   flutter pub get
   ```

3. Запустите приложение:

   ```bash
   flutter run
   ```

## 📋 Функциональность

### ✅ Реализовано

- **Список чатов** с аватарами и непрочитанными сообщениями
- **Экран чата** с пузырьками сообщений
- **Отправка сообщений** в реальном времени
- **Обновление последнего сообщения** в списке чатов
- **Отметка чатов как прочитанных**
- **Навигация между экранами**
- **Локализация** (русский/английский)

### 🔄 Управление состоянием

Приложение использует **Clean Architecture** с разделением ответственности:

#### Infrastructure Layer (Riverpod провайдеры)

- `chatProvider` - провайдер для ChatNotifier
- `messageProvider` - провайдер для MessageNotifier

#### Presentation Layer (Стейт менеджеры)

- `ChatNotifier` - управление списком чатов
- `MessageNotifier` - управление сообщениями в чате

### 🎨 Дизайн

- **Минималистичный** интерфейс в стиле Cupertino
- **Адаптивные** компоненты под размер экрана
- **Нативные** жесты и анимации iOS
- **Темная/светлая** тема (системная)

## 🏛️ Dependency Injection

Все зависимости централизованы в папке `infrastructure/di/`:

### Использование

```dart
import '../infrastructure/di/di.dart';
import '../presentation/presentation.dart';

class MyWidget extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final chatNotifier = ref.watch<ChatNotifier>(chatProvider);
    // ...
  }
}
```

### Преимущества DI структуры

1. **Централизация** - все зависимости в одном месте
2. **Масштабируемость** - легко добавлять новые провайдеры
3. **Тестируемость** - простое мокирование зависимостей
4. **Чистота кода** - четкое разделение ответственности

## 📱 Экраны

### Список чатов

- Отображение всех чатов с последними сообщениями
- Индикаторы непрочитанных сообщений
- Кнопка добавления нового чата
- Навигация к экрану чата

### Экран чата

- Интерфейс переписки с пузырьками сообщений
- Поле ввода с кнопкой отправки
- Аватары собеседников
- Время отправки сообщений

## 🔧 Разработка

### Добавление нового провайдера

1. Добавьте провайдер в `infrastructure/di/providers.dart`
2. Экспортируйте его в `infrastructure/di/di.dart`
3. Используйте в экранах через `ref.watch()` или `ref.read()`

### Добавление нового сервиса

1. Добавьте сервис в `infrastructure/di/services.dart`
2. Создайте провайдер для сервиса
3. Экспортируйте в `infrastructure/di/di.dart`

## 📄 Лицензия

Этот проект создан для образовательных целей.
