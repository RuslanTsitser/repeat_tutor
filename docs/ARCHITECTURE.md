# Архитектура приложения

Этот документ описывает архитектурные решения и принципы, используемые в приложении Repeat Tutor.

## 🏗️ Общая архитектура

Приложение построено на основе **Clean Architecture** с использованием **MVVM** паттерна и **Riverpod** для управления состоянием.

### Слои архитектуры

```text
┌─────────────────────────────────────┐
│           Presentation Layer        │  ← Экраны, виджеты, нотаторы
├─────────────────────────────────────┤
│           Domain Layer              │  ← Модели, интерфейсы репозиториев
├─────────────────────────────────────┤
│           Data Layer                │  ← Реализации репозиториев
├─────────────────────────────────────┤
│           Infrastructure Layer      │  ← DI, сервисы, провайдеры
└─────────────────────────────────────┘
```

## 📱 Presentation Layer

### Экраны (Screens)
- `ChatListScreen` - отображение списка чатов
- `ChatScreen` - интерфейс переписки

### Виджеты (Widgets)
- `ChatListItem` - элемент списка чатов
- `MessageBubble` - пузырек сообщения
- `MessageInput` - поле ввода сообщений

### Принципы
- Использование `ConsumerWidget` и `ConsumerStatefulWidget`
- Минимальная бизнес-логика в UI
- Реактивное обновление через Riverpod

## 🎯 Domain Layer

### Модели (Models)
- `Chat` - модель чата с Equatable
- `Message` - модель сообщения с Equatable

### Принципы
- Чистые классы данных
- Неизменяемые объекты (immutable)
- Сравнение через Equatable

## 🛠️ Infrastructure Layer

### Dependency Injection
- Централизованное управление зависимостями в `di.dart`
- Провайдеры репозиториев и нотаторов
- Конфигурация зависимостей

### Провайдеры
- `chatRepositoryProvider` - провайдер для ChatRepository
- `messageRepositoryProvider` - провайдер для MessageRepository
- `chatProvider` - провайдер для ChatNotifier
- `messageProvider` - провайдер для MessageNotifier

## 💾 Data Layer

### Репозитории
- `ChatRepositoryImpl` - реализация репозитория чатов
- `MessageRepositoryImpl` - реализация репозитория сообщений

### Принципы
- Реализация интерфейсов из domain слоя
- Временное хранилище в памяти (готово для замены на БД)
- Асинхронные операции с имитацией сетевых задержек

## 🔄 Управление состоянием

### Riverpod + ChangeNotifier

```dart
// Провайдер
final chatListProvider = ChangeNotifierProvider<ChatListNotifier>((ref) {
  return ChatListNotifier();
});

// Notifier
class ChatListNotifier extends ChangeNotifier {
  List<Chat> _chats = [];
  
  List<Chat> get chats => _chats;
  
  void updateChats(List<Chat> newChats) {
    _chats = newChats;
    notifyListeners();
  }
}

// Использование
final chatList = ref.watch<ChatListNotifier>(chatListProvider);
```

### Принципы
- Единый источник истины для каждого состояния
- Реактивные обновления UI
- Типобезопасность

## 🎨 UI/UX принципы

### Cupertino Design
- Нативный iOS интерфейс
- Минималистичный дизайн
- Плавные анимации

### Адаптивность
- Responsive компоненты
- Поддержка разных размеров экрана
- Accessibility

## 📦 Структура папок

```
lib/
├── data/                   # Data слой
│   └── repositories/       # Реализации репозиториев
├── domain/                 # Domain слой
│   ├── models/             # Доменные модели
│   └── repositories/       # Интерфейсы репозиториев
├── infrastructure/         # Infrastructure слой
│   └── di.dart            # Dependency Injection
├── presentation/           # Presentation слой
│   ├── notifiers/         # Нотаторы состояния
│   ├── screens/           # Экраны приложения
│   └── widgets/           # Переиспользуемые виджеты
├── localization/           # Локализация
└── main.dart              # Точка входа
```

## 🔧 Принципы разработки

### SOLID принципы
- **S** - Single Responsibility Principle
- **O** - Open/Closed Principle
- **L** - Liskov Substitution Principle
- **I** - Interface Segregation Principle
- **D** - Dependency Inversion Principle

### Clean Code
- Понятные имена переменных и функций
- Короткие функции с одной ответственностью
- Минимальная вложенность кода
- Комментарии только там, где необходимо

### DRY (Don't Repeat Yourself)
- Переиспользуемые виджеты
- Централизованная логика в провайдерах
- Общие утилиты и хелперы

### Импорты и экспорты
- **НЕ создавать файлы экспортов** - все импорты должны быть прямыми и явными
- Использовать относительные импорты для лучшей читаемости
- Группировать импорты: сначала внешние пакеты, затем внутренние модули

## 🧪 Тестирование

### Стратегия тестирования
- Unit тесты для провайдеров
- Widget тесты для UI компонентов
- Integration тесты для пользовательских сценариев

### Мокирование
- Мокирование провайдеров через Riverpod overrides
- Изоляция тестов от внешних зависимостей

## 🚀 Производительность

### Оптимизации
- Lazy loading провайдеров
- Минимальные rebuilds через ConsumerWidget
- Эффективное управление памятью

### Мониторинг
- Отслеживание производительности через Flutter DevTools
- Профилирование состояния приложения

## 📈 Масштабируемость

### Расширение функциональности
- Добавление новых провайдеров
- Создание новых экранов
- Интеграция с внешними API

### Рефакторинг
- Модульная архитектура
- Слабая связанность компонентов
- Легкое тестирование изменений

## 🔒 Безопасность

### Принципы
- Валидация входных данных
- Безопасное хранение данных
- Защита от утечек памяти

### Лучшие практики
- Использование const конструкторов
- Правильное управление жизненным циклом
- Очистка ресурсов в dispose()

